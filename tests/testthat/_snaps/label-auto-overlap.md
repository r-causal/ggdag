# the unresolved warning reads in the package's cli style

    Code
      expr
    Condition <ggdag_label_unresolved_warning>
      Warning in `warn_unresolved_labels()`:
      3 labels could not be placed clear of the drawing: "Weight", "Blood pressure", and "Outcome".
      i Draw the plot on a larger device, shorten the labels or wrap them with `wrap`, or leave them out with `max.overlaps = 0`.

