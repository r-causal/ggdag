# the repel label geoms name the aesthetic they are missing

    Code
      expr
    Condition <rlang_error>
      Error in `label_geom()`:
      ! Problem while computing stat.
      i Error occurred in the 1st layer.
      Caused by error:
      ! The repel label geoms need the DAG aesthetics on the plot.
      x The layer does not set x.
      i Build the plot with `ggplot(dag, aes_dag())`.

