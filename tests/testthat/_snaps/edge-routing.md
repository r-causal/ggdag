# auto_route option validation errors are informative

    Code
      expr
    Condition <ggdag_type_error>
      Error in `ggdag_options_set()`:
      ! `auto_route` must be a single logical value (TRUE or FALSE).
      x You provided a string.

---

    Code
      expr
    Condition <ggdag_type_error>
      Error in `ggdag_options_set()`:
      ! `auto_route` must be a single logical value (TRUE or FALSE).
      x You provided a number.

---

    Code
      expr
    Condition <ggdag_type_error>
      Error in `ggdag_options_set()`:
      ! `auto_route` must be a single logical value (TRUE or FALSE).
      x You provided `NA`.

