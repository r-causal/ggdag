# Convert a `tidy_dagitty` object to tbl_df

Convert a `tidy_dagitty` object to tbl_df

## Usage

``` r
tbl_df.tidy_dagitty(.tdy_dag)
```

## Arguments

- .tdy_dag:

  A `tidy_dagitty` or `dagitty` object
