% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dagify.R
\name{curve_edge}
\alias{curve_edge}
\title{Add or update curvature for a single edge}
\usage{
curve_edge(.dag, from, to, curvature = 0.3)
}
\arguments{
\item{.dag}{A \code{dagitty} or \code{tidy_dagitty} object.}

\item{from}{Character. The name of the source node.}

\item{to}{Character. The name of the target node.}

\item{curvature}{Numeric. The curvature value for the edge.}
}
\value{
The modified \code{.dag} object with updated curvature.
}
\description{
\code{curve_edge()} sets the curvature for a single edge on a \code{dagitty} or
\code{tidy_dagitty} object. Use \code{\link[=set_curve_edges]{set_curve_edges()}} to set multiple edges at once.
}
\section{Curvature sign convention}{

The curvature value is passed directly to the active edge rendering engine.
The \strong{ggraph} engine (default) and \strong{ggarrow} engine interpret the sign
differently:
\itemize{
\item \strong{ggraph}: positive curvature curves \emph{above} (to the left of) a
left-to-right edge.
\item \strong{ggarrow} / \strong{grid}: positive curvature curves \emph{below} (to the right
of) a left-to-right edge, following \code{grid::curveGrob()} convention.
}

This means the same \code{curvature} value will render as a mirror image
depending on the engine. ggdag does not negate or transform the value;
each engine uses its native convention.
}

\examples{
dag <- dagify(y ~ x + m, m ~ x)
dag <- curve_edge(dag, from = "m", to = "y", curvature = 0.5)

}
