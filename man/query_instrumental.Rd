% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_functions.R
\name{query_instrumental}
\alias{query_instrumental}
\title{Query Instrumental Variables}
\usage{
query_instrumental(
  .tdy_dag,
  exposure = NULL,
  outcome = NULL,
  conditioned_on = NULL
)
}
\arguments{
\item{.tdy_dag}{A tidy DAG object.}

\item{exposure}{Character vector of exposure variable names. If NULL,
uses the exposure defined in the DAG.}

\item{outcome}{Character vector of outcome variable names. If NULL,
uses the outcome defined in the DAG.}

\item{conditioned_on}{Character vector of variables that must be conditioned on.}
}
\value{
A tibble with columns:
\itemize{
\item \code{instrument}: The instrumental variable
\item \code{exposure}: The exposure variable
\item \code{outcome}: The outcome variable
\item \code{conditioned_on}: List column of required conditioning variables
}
}
\description{
Identify instrumental variables for a given exposure-outcome pair.
}
\examples{
library(ggdag)
dag <- dagify(
  y ~ x + u,
  x ~ z + u,
  exposure = "x",
  outcome = "y",
  latent = "u"
)

query_instrumental(dag)
}
