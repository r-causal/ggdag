% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_functions.R
\name{query_paths}
\alias{query_paths}
\title{Query Paths in a DAG}
\usage{
query_paths(
  .tdy_dag,
  from = NULL,
  to = NULL,
  directed = FALSE,
  limit = 100,
  conditioned_on = NULL
)
}
\arguments{
\item{.tdy_dag}{A tidy DAG object.}

\item{from}{Character vector of starting nodes. If NULL, uses exposure from DAG.}

\item{to}{Character vector of ending nodes. If NULL, uses outcome from DAG.}

\item{directed}{Logical. If TRUE, only considers directed paths.}

\item{limit}{Maximum number of paths to return. Default is 100.}

\item{conditioned_on}{Character vector of variables to condition on.}
}
\value{
A tibble with columns:
\itemize{
\item \code{path_id}: Integer identifier for each path
\item \code{from}: Starting node
\item \code{to}: Ending node
\item \code{path}: Character string representation of the path
\item \code{open}: Logical indicating if the path is open
}
}
\description{
Find all paths between specified nodes in a DAG and determine if they are
open or closed given a conditioning set.
}
\examples{
library(ggdag)
dag <- dagify(
  y ~ x + z,
  x ~ w,
  z ~ w,
  exposure = "x",
  outcome = "y"
)

query_paths(dag)
query_paths(dag, conditioned_on = "z")
}
