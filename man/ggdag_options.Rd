% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{ggdag_defaults}
\alias{ggdag_defaults}
\alias{ggdag_options_set}
\alias{ggdag_options_get}
\alias{ggdag_options_reset}
\alias{ggdag_option}
\alias{ggdag_option_proportional}
\title{Global DAG Options}
\usage{
ggdag_defaults

ggdag_options_set(...)

ggdag_options_get(name = NULL)

ggdag_options_reset()

ggdag_option(name, default)

ggdag_option_proportional(name, base_default, override_default)
}
\arguments{
\item{...}{Named option values to set. See \code{ggdag_defaults} for valid names
and types.}

\item{name}{Character string. The option name (without the \code{ggdag.} prefix).
If \code{NULL}, returns all currently-set ggdag options.}

\item{default}{Default value to return if the option is not set.}

\item{base_default}{The base default for this option (e.g., 8 for edge_cap).}

\item{override_default}{The override default used by certain functions
(e.g., 10 for edge_cap in adjustment set functions).}
}
\value{
\itemize{
\item \code{ggdag_options_set()}: Invisibly returns a named list of the previous
option values.
\item \code{ggdag_options_get()}: The option value, or a named list of all set
options if \code{name} is \code{NULL}.
\item \code{ggdag_options_reset()}: Called for its side effect; returns \code{NULL}
invisibly.
\item \code{ggdag_option()}: The option value if set, otherwise \code{default}.
\item \code{ggdag_option_proportional()}: The scaled option value if set, otherwise
\code{override_default}.
}
}
\description{
Set, get, and reset global default options for DAG appearance. These options
are used as defaults by all \code{geom_dag()}, \verb{ggdag_*()}, and related functions.
}
\details{
Options are stored in R's global \code{\link[=options]{options()}} as \verb{ggdag.<name>}. When an
option is \code{NULL} (the default), each function uses its own built-in
default. Setting a global option overrides the built-in default for all
functions that use it. Passing \code{NULL} to \code{ggdag_options_set()} leaves that
one option unset, which returns it to the built-in default and leaves every
other option alone. That also makes the previous values
\code{ggdag_options_set()} returns safe to restore with
\code{do.call(ggdag_options_set, old)}.

Functions that normally use \code{edge_cap = 10} (e.g., \code{\link[=ggdag_adjustment_set]{ggdag_adjustment_set()}},
\code{\link[=ggdag_drelationship]{ggdag_drelationship()}}) maintain a proportional offset. If you set
\code{ggdag.edge_cap} to a custom value, these functions scale it by \code{10/8}.

\code{auto_curve} is a safety net for layouts where a node sits on or near the
straight path of an edge. When it is \code{TRUE}, \code{\link[=tidy_dagitty]{tidy_dagitty()}} checks every
straight directed edge against the other nodes of the DAG and writes a
per-edge curvature that routes each blocked edge around them, bowing away
from the side where the intruding nodes sit. Curvature you set yourself,
through \code{\link[=curved]{curved()}}, \code{\link[=curve_edge]{curve_edge()}}, or DAGitty control points, is never
overridden, and bidirected edges keep the arc their edge layer draws them
with. The written curvature is drawn by the ggarrow edge engine, so pair it
with \code{edge_engine = "ggarrow"} to see the routed edges.

\code{auto_route} swaps routed edges into the packaged ggarrow edge rendering.
When it is \code{TRUE} and \code{edge_engine = "ggarrow"}, \code{\link[=geom_dag]{geom_dag()}} and the
plots built on it, such as \code{\link[=ggdag]{ggdag()}}, draw their directed edges with the
routed layer of \code{\link[=geom_dag_routed_arrows]{geom_dag_routed_arrows()}} under the default
\code{edge_type = "link_arc"}, so an edge whose straight path a node blocks
detours around that node instead of running underneath it. Bidirected
edges keep the arc their edge layer draws them with, unblocked edges stay
straight, and curvature you set yourself is never rerouted. The option
only affects the ggarrow engine, so pair it with
\code{edge_engine = "ggarrow"}.

\code{debug_repel_points} is a diagnostic rather than an appearance setting. When
it is \code{TRUE}, every repelling label geom (see \code{\link[=geom_dag_label_repel]{geom_dag_label_repel()}}) adds
a layer of purple points showing the invisible geometry that labels are
repelled from: the points traced along each edge and the disc filling each
node. It is useful for understanding why a label came to rest where it did.
}
\examples{
# Set global options
old <- ggdag_options_set(node_size = 20, text_size = 5)

# Check current value
ggdag_options_get("node_size")

# Reset to defaults
ggdag_options_reset()

}
