% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_dag_arrow.R
\name{geom_dag_routed_arrows}
\alias{geom_dag_routed_arrows}
\title{Routed DAG edges that detour around nodes}
\usage{
geom_dag_routed_arrows(
  mapping = NULL,
  data_directed = filter_direction("->"),
  data_bidirected = filter_direction("<->"),
  node_radius = node_radius_data(),
  curvature = 0.3,
  arrow_head = ggarrow::arrow_head_wings(),
  arrow_fins = NULL,
  arrow_mid = NULL,
  length = 4,
  length_head = NULL,
  length_fins = NULL,
  length_mid = NULL,
  justify = 0,
  force_arrow = FALSE,
  mid_place = 0.5,
  resect = NULL,
  resect_head = NULL,
  resect_fins = NULL,
  lineend = "butt",
  linejoin = "round",
  linemitre = 10,
  position = "identity",
  na.rm = TRUE,
  show.legend = NA,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}},
applied to the bidirected arc layer.}

\item{data_directed, data_bidirected}{The data to be displayed for directed
and bidirected edges respectively. By default, these filter the plot
data by edge direction.}

\item{node_radius}{Drawn node radius in data units, the scale on which the
routing works: an edge is blocked when a node sits strictly within
1.5 node radii of its straight path, and the routed path clears the
obstacle by the same expanded radius. Defaults to the radius the
default-size node is drawn at.}

\item{curvature}{A numeric value giving the amount of curvature. Negative
values produce left-hand curves, positive values produce right-hand curves,
and zero produces a straight line.}

\item{arrow_head, arrow_fins, arrow_mid}{Arrow ornament functions from
ggarrow (e.g., \code{ggarrow::arrow_head_wings()},
\code{ggarrow::arrow_head_line()}). Set to \code{NULL} to suppress an ornament.}

\item{length, length_head, length_fins, length_mid}{Size of arrow ornaments.
A numeric value sets the size relative to \code{linewidth}; a
\code{\link[grid:unit]{grid::unit()}} sets an absolute size.}

\item{justify}{A numeric value between 0 and 1 controlling where the arrow
is drawn relative to the path endpoints. 0 (default) places the tip at
the endpoint; 1 places the base at the endpoint.}

\item{force_arrow}{If \code{TRUE}, draw arrows even when the path is shorter
than the arrow ornaments. Default \code{FALSE}.}

\item{mid_place}{Numeric vector with values between 0 and 1 setting
positions for interior arrows, or a \code{\link[grid:unit]{grid::unit()}} for spacing.}

\item{resect}{A numeric value in millimetres to shorten the arrow from both
ends, or \code{NULL} (the default) to leave both ends to auto-resection.
Overridden by \code{resect_head}/\code{resect_fins} if set. \code{0} is a value like any
other: it turns auto-resection off and draws the edge up to the node.}

\item{resect_head, resect_fins}{Numeric values in millimetres to shorten the
arrow from the head or fins end respectively, or \code{NULL} (the default) to
leave that end to auto-resection.}

\item{lineend}{Line end style: \code{"butt"} (default), \code{"round"}, or
\code{"square"}.}

\item{linejoin}{Line join style: \code{"round"} (default), \code{"mitre"}, or
\code{"bevel"}.}

\item{linemitre}{Line mitre limit (default 10).}

\item{position}{Position adjustment, either as a string or the result of a
call to a position adjustment function.}

\item{na.rm}{If \code{FALSE}, removes missing values with a warning. If \code{TRUE}
(the default for DAG geoms), silently removes missing values.}

\item{show.legend}{Logical. Should this layer be included in the legends?}

\item{...}{Other arguments passed on to the layer.}
}
\value{
A list of \code{\link[ggplot2:layer]{ggplot2::layer()}} objects that can be added to a plot.
}
\description{
\code{geom_dag_routed_arrows()} draws DAG edges with the ggarrow engine,
routing every directed edge whose straight path a node blocks around that
node. Each blocked edge takes the shortest path through a visibility graph
built over the tangent points of the obstacle circles, expanded to
1.5 node radii so the drawn path keeps a clearance margin of half a node
radius, and the corners are smoothed with two passes of Chaikin corner
cutting. Unblocked edges stay straight, bidirected edges are drawn as arcs
by the same curve geom \code{\link[=geom_dag_arrows]{geom_dag_arrows()}} uses, and the routing is
deterministic: the same DAG always draws the same paths.
}
\details{
Curvature the user set is never rerouted. When the data carries an
\code{edge_curvature} column, from \code{\link[=curved]{curved()}}, \code{\link[=curve_edge]{curve_edge()}}, or your own
code, an edge with a numeric curvature follows that arc, an explicit 0
stays straight through whatever sits on its chord, and only edges whose
curvature is unset (\code{NA}) are candidates for routing.

The routed layer computes its waypoints from the layer data, so it does
not inherit the plot's aesthetic mapping; the \code{mapping} argument applies
to the bidirected arc layer. Edges are resected to the plot's node size
exactly as in \code{\link[=geom_dag_arrow]{geom_dag_arrow()}}.
}
\examples{
library(ggplot2)
dag <- dagify(
  y ~ x + m,
  m ~ x,
  coords = list(x = c(x = 0, m = 1, y = 2), y = c(x = 0, m = 0, y = 0))
)

# the x -> y edge detours around the mediator sitting on its path
dag |>
  ggplot(aes_dag()) +
  geom_dag_routed_arrows() +
  geom_dag_point() +
  geom_dag_text() +
  theme_dag()

}
\seealso{
\code{\link[=geom_dag_arrow]{geom_dag_arrow()}}, \code{\link[=geom_dag_arrows]{geom_dag_arrows()}}, and \code{\link[=geom_dag_edges]{geom_dag_edges()}}
for the other edge geoms, and the \code{auto_route} option in
\code{\link[=ggdag_options_set]{ggdag_options_set()}} to swap routed edges into \code{geom_dag()} and the
quick plotting functions.
}
