% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_dag_arrow.R
\name{geom_dag_routed_arrows}
\alias{geom_dag_routed_arrows}
\title{Routed DAG edges that detour around nodes}
\usage{
geom_dag_routed_arrows(
  mapping = NULL,
  data_directed = filter_direction("->"),
  data_bidirected = filter_direction("<->"),
  route = c("spline", "orthogonal"),
  clearance = NULL,
  edge_sep = NULL,
  edge_sep_min = NULL,
  edge_route_options = NULL,
  layer_axis = c("auto", "x", "y"),
  node_size = NULL,
  curvature = 0.3,
  arrow_head = ggarrow::arrow_head_wings(),
  arrow_fins = NULL,
  arrow_mid = NULL,
  length = 4,
  length_head = NULL,
  length_fins = NULL,
  length_mid = NULL,
  justify = 0,
  force_arrow = FALSE,
  mid_place = 0.5,
  resect = NULL,
  resect_head = NULL,
  resect_fins = NULL,
  lineend = "butt",
  linejoin = "round",
  linemitre = 10,
  position = "identity",
  na.rm = TRUE,
  show.legend = NA,
  inherit.aes = TRUE,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}},
applied to both the routed layer and the bidirected arc layer.}

\item{data_directed, data_bidirected}{The data to be displayed for directed
and bidirected edges respectively. By default, these filter the plot
data by edge direction.}

\item{route}{How to route the edges: \code{"spline"} (the default) draws a
blocked edge as a smooth curve around the obstacle and leaves unblocked
edges straight. \code{"orthogonal"} draws every edge as axis-aligned runs
with rounded corners, passing the intermediate layers in the gaps
between them or along a channel beyond them.}

\item{clearance}{The daylight in millimetres a routed path keeps beyond
the node discs, or \code{NULL} (the default) for the router's own margin,
half a node radius with a floor of 1.2 mm.}

\item{edge_sep}{The gap in millimetres between two routed paths sharing a
detour, or \code{NULL} (the default) for the router's own separation.}

\item{edge_sep_min}{The gap in millimetres the orthogonal router may
tighten \code{edge_sep} to when a gap between layers is too narrow for its
slots at the full separation, or \code{NULL} (the default) for a quarter of
the node radius with a floor of 1.5 mm. A value above a separation the
router derives for itself is reduced to it; a value above an \code{edge_sep}
you set as well is an error. Set it equal to \code{edge_sep} to keep the
separation fixed. Spline routing does not use it.}

\item{edge_route_options}{An object from \code{\link[=edge_route_options]{edge_route_options()}} carrying
the rest of the constants the router draws with, or \code{NULL} (the default)
for the router's own. \code{clearance}, \code{edge_sep}, and \code{edge_sep_min} above
override the object's fields of those names for this layer.}

\item{layer_axis}{The axis the layout's layers run along, one of \code{"auto"}
(the default), \code{"x"}, or \code{"y"}. Routing sends a detour along the
within-layer axis, so a layout laid out down the panel rather than across
it needs the axis it runs along. Under \code{"auto"}, a layout that ordered
time down the panel, such as \code{\link[=time_ordered_coords]{time_ordered_coords()}} with
\code{direction = "y"}, is taken at its word, and every other scene has its
layers inferred from the node positions. The direction is read from the
DAG the plot is drawn from, which carries the axis the layout recorded;
name the axis yourself for a layout ggdag did not compute.}

\item{node_size}{The size of the drawn nodes, in the units
\code{\link[=geom_dag_point]{geom_dag_point()}} takes, giving the router the radius of the discs it
clears. \code{NULL}, the default, takes it from the plot's node layer.}

\item{curvature}{A numeric value giving the amount of curvature. Negative
values produce left-hand curves, positive values produce right-hand curves,
and zero produces a straight line.}

\item{arrow_head, arrow_fins, arrow_mid}{Arrow ornament functions from
ggarrow (e.g., \code{ggarrow::arrow_head_wings()},
\code{ggarrow::arrow_head_line()}). Set to \code{NULL} to suppress an ornament.}

\item{length, length_head, length_fins, length_mid}{Size of arrow ornaments.
A numeric value sets the size relative to \code{linewidth}; a
\code{\link[grid:unit]{grid::unit()}} sets an absolute size.}

\item{justify}{A numeric value between 0 and 1 controlling where the arrow
is drawn relative to the path endpoints. 0 (default) places the tip at
the endpoint; 1 places the base at the endpoint.}

\item{force_arrow}{If \code{TRUE}, draw arrows even when the path is shorter
than the arrow ornaments. Default \code{FALSE}.}

\item{mid_place}{Numeric vector with values between 0 and 1 setting
positions for interior arrows, or a \code{\link[grid:unit]{grid::unit()}} for spacing.}

\item{resect}{A numeric value in millimetres to shorten the arrow from both
ends, or \code{NULL} (the default) to leave both ends to auto-resection.
Overridden by \code{resect_head}/\code{resect_fins} if set. \code{0} is a value like any
other: it turns auto-resection off and draws the edge up to the node.}

\item{resect_head, resect_fins}{Numeric values in millimetres to shorten the
arrow from the head or fins end respectively, or \code{NULL} (the default) to
leave that end to auto-resection.}

\item{lineend}{Line end style: \code{"butt"} (default), \code{"round"}, or
\code{"square"}.}

\item{linejoin}{Line join style: \code{"round"} (default), \code{"mitre"}, or
\code{"bevel"}.}

\item{linemitre}{Line mitre limit (default 10).}

\item{position}{Position adjustment, either as a string or the result of a
call to a position adjustment function.}

\item{na.rm}{If \code{FALSE}, removes missing values with a warning. If \code{TRUE}
(the default for DAG geoms), silently removes missing values.}

\item{show.legend}{Logical. Should this layer be included in the legends?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics rather than
combining with them.}

\item{...}{Other arguments passed on to the layer.}
}
\value{
A list of \code{\link[ggplot2:layer]{ggplot2::layer()}} objects that can be added to a plot.
}
\description{
\code{geom_dag_routed_arrows()} draws DAG edges with the ggarrow engine,
routing every directed edge whose path a node blocks around that node.
The routing happens when the plot is drawn, in the millimetres of the
device, so the detour clears the drawn node discs whatever the shape of
the panel and the picture re-routes when the plot is resized. Unblocked
edges stay straight, bidirected edges are drawn as arcs by the same curve
geom \code{\link[=geom_dag_arrows]{geom_dag_arrows()}} uses, and the routing is deterministic: the same
DAG at the same size always draws the same paths. In spline mode an
unblocked edge that would run through another edge's drawn arrowhead is
nudged past it by a few millimetres, so it still reads as straight and
the arrowhead is drawn whole.
}
\details{
Curvature the user set is never rerouted. When the data carries an
\code{edge_curvature} column, from \code{\link[=curved]{curved()}}, \code{\link[=curve_edge]{curve_edge()}}, or your own
code, an edge with a numeric curvature is drawn as that arc, an explicit
\code{0} stays straight through whatever sits on its chord, and only edges
whose curvature is unset (\code{NA}) are candidates for routing. An arc the
user set still counts as an obstacle the other edges route around.

The layer carries every row of the plot data, so the router can treat
every drawn node as an obstacle, and draws the rows \code{data_directed}
selects. Edge rows of a data frame you supply that are not among the plot
rows are drawn as well, and their endpoints join the obstacles. Scales and
legends therefore see exactly what the other DAG layers see. Edges are
resected to the plot's node size exactly as in \code{\link[=geom_dag_arrow]{geom_dag_arrow()}}, and the
same node size gives the router the radius of the discs it must clear. The
automatic label geoms take their obstacles from the plot's own nodes, so a
label may be placed across an edge appended this way.

A routed path is stroked at one width along its length, so
\code{linewidth_head} and \code{linewidth_fins} taper only the arcs drawn for
curvature the user set.
}
\examples{
library(ggplot2)
dag <- dagify(
  y ~ x + m,
  m ~ x,
  coords = list(x = c(x = 0, m = 1, y = 2), y = c(x = 0, m = 0, y = 0))
)

# the x -> y edge detours around the mediator sitting on its path
dag |>
  ggplot(aes_dag()) +
  geom_dag_routed_arrows() +
  geom_dag_point() +
  geom_dag_text() +
  theme_dag()

}
\seealso{
\code{\link[=geom_dag_arrow]{geom_dag_arrow()}}, \code{\link[=geom_dag_arrows]{geom_dag_arrows()}}, and \code{\link[=geom_dag_edges]{geom_dag_edges()}}
for the other edge geoms, and the \code{edge_route} option in
\code{\link[=ggdag_options_set]{ggdag_options_set()}} to swap routed edges into \code{geom_dag()} and the
quick plotting functions.
}
